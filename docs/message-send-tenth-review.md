# #42 第十轮复核：2c1b3d4

## 结论

本轮只有测试，没有新的产品行为。第九轮点名的「空数组 vs 缺字段」已经钉住，账本和热投影两条路都有断言。我没有再找到发送所有权上的新缺陷。

**主路径可以停手。** 是否关 #42 由你定；下面四条应作为已知限制留下，不要写成未修 bug。

基线：HEAD `2c1b3d4`。未改业务代码。`tsc --noEmit` 通过。本轮定向 `queue-state` + `useAgentSessionQueue` **37/37**。

## 本轮核对

新增用例确实锁的是第九轮说的语义，不是空转：

- `lib/queue-state.test.mjs`：缺 `admittedAttemptIds` 保持账本令牌；显式 `[]` 覆盖。
- `hooks/useAgentSessionQueue.test.mjs`：`normalizeQueuedMessages` 缺字段 → `undefined`、非字符串丢掉、空数组原样；再经 `applyProjectedQueues`，缺字段不清空、空数组才清空。

K2 产品路径（热投影带令牌、再入队不多一条同文）上一轮已绿，本轮仍绿。

未改你的第八轮探针：它喂给账本的是「不带令牌的快照」，绿表示账本在缺信息时无法判断，不是产品回退。等价验收以仓库 K2 为准。

## 已知限制（不要当新缺陷重开）

1. 显式 `[]` 会清空账本令牌——这是服务端事实；只有错发空数组才会误伤。
2. J2：无 id 时按正文+附件配对，避免同文重复条目；主路径带真 id。
3. 跨崩溃不是 exactly-once；unknown 是待确认，要用户处置。
4. 没有双标签浏览器无头基建；多标签语义由真实 Host 交错覆盖。

## 建议

接受上述限制就可以关 #42。本轮不修改业务代码，也不代关 Issue。
